import { View, Text, TouchableOpacity } from 'react-native'
import React from 'react'

const Country = ({item}) => {
  return (
    <TouchableOpacity>
        <View>
            
        </View>
    </TouchableOpacity>
  )
}

export default Country